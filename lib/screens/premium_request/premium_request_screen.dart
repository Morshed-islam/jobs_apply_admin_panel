import 'package:flutter/material.dart';

class PremiumRequestPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Premium Here',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}