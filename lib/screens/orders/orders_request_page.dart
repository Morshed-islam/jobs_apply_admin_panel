import 'package:flutter/material.dart';

class OrdersRequestPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Order request ',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}